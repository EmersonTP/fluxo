export type Member = { id: string; name: string; color: string; role: string };
export type Area = { id: string; name: string; color: string };
export type Cfg = { id: string; role: string; spaceId: string | null; userId: string };
export type Credor = { id: string; nome: string; documento: string; tipo: string; pixKey?: string | null; categoriaPadrao?: string | null };
